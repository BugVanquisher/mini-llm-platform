# Core API
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1

# HTTP + env
httpx==0.27.0
python-dotenv==1.0.1

# Optional: use OpenAI as an alternate provider
openai==1.40.6

# (We'll add prometheus-client and deepeval later)