# Core API
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1

# Model serving
vllm==0.5.0  # requires CUDA, PyTorch will be auto-installed with it

# Monitoring (phase 1 extension)
prometheus-client==0.20.0

# Evaluation (phase 1 extension)
deepeval==0.20.54
openai==1.40.6